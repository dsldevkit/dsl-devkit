/*/bin/
/*/bin-res/
/*/target/
**/.tycho-consumer-pom.xml
*.java._trace
*.smap
*.checkbin

# macOS
.DS_Store

# Local mirror of .agents/skills/ created by .agents/sync.sh — do not commit
# Match both the directory form (Windows copy) and the symlink form (macOS/Linux).
/.claude/skills
/.claude/skills/
