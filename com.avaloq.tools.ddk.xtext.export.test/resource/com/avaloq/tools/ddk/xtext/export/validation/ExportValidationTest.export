import "http://www.eclipse.org/emf/2002/Ecore" as ecore

extension XXX

export ecore::EClass as name
{
    data intl_id = this.getIntlId();
    // TODO add more data, e.g. intl2_id, user_id(?), ...
}

export ecore::EClass as name
{
    field instanceClassName;
    field instanceClassName;
    data intl_id = this.getSomeValue();
    data instanceClassName = this.getSomeValue();
    // TODO add more data, e.g. intl2_id, user_id(?), ...
}

export ecore::EPackage as nsURI
{
    field nsURI;
    // TODO add more data, e.g. intl2_id, user_id(?), ...
}
